/**
 * @module Platform API
 *
 * Plugins, tenants, cloud, secrets, and SSO.
 *
 * Currently a placeholder — platform-specific endpoints will be
 * added as the API expands. Re-exports nothing yet but exists to
 * maintain the intended module structure.
 */
